\chapter{实验结果与统计检验}

本章在第~2~章定义的固定划分、模型矩阵和评价指标基础上展示主实验结果。除特别说明外，所有数值均来自已同步的主实验聚合表、低信噪比分组表、受控 CUDA 前向延迟表以及配对统计检验表。本章不新增训练、不重建主表，也不把诊断性输出纳入性能结论。

\section{主实验整体结果}

表~\ref{tab:course-main-results-excerpt} 汇总 9 个模型行在 3 个训练种子上的平均结果。该表同时列出整体准确率、宏平均 F1、均衡准确率、低/中/高信噪比分组准确率和参数量，用于从总体性能、信噪比分组和模型规模三个角度观察结果。

\begin{table}[htbp]
  \centering
  \caption{固定划分主实验结果（三训练种子均值与标准差）}
  \label{tab:course-main-results-excerpt}
  \scriptsize
  \setlength{\tabcolsep}{2.5pt}
  \resizebox{\textwidth}{!}{%
  \begin{tabular}{llccccccr}
    \toprule
    模型行 & 输入视图 & 整体准确率 & 宏平均 F1 & 均衡准确率 & 低信噪比准确率 & 中信噪比准确率 & 高信噪比准确率 & 参数量 \\
    \midrule
    \path{cnn1d} & I/Q\cite{oshea2016cnn} & $0.581992\pm0.004518$ & $0.595238\pm0.003937$ & $0.581992\pm0.004518$ & $0.209508\pm0.005465$ & $0.795606\pm0.001772$ & $0.865025\pm0.006640$ & 37,131 \\
    \path{resnet1d} & I/Q\cite{he2016resnet} & $0.595917\pm0.000451$ & $0.612207\pm0.000668$ & $0.595917\pm0.000451$ & $0.205871\pm0.002523$ & $0.819722\pm0.004139$ & $0.892172\pm0.001495$ & 111,755 \\
    \path{tfcnn_stft} & STFT & $0.506303\pm0.001752$ & $0.509340\pm0.006411$ & $0.506303\pm0.001752$ & $0.171307\pm0.003870$ & $0.689975\pm0.005485$ & $0.769293\pm0.003574$ & 24,123 \\
    \path{fusion_iq_stft} & I/Q+STFT & $0.577098\pm0.001712$ & $0.584926\pm0.003526$ & $0.577098\pm0.001712$ & $\mathbf{0.221326\pm0.006128}$ & $0.785808\pm0.006158$ & $0.842753\pm0.005760$ & 98,299 \\
    \path{cldnn}\cite{sainath2015cldnn} & I/Q & $\mathbf{0.612932\pm0.000546}$ & $\mathbf{0.633238\pm0.003378}$ & $\mathbf{0.612932\pm0.000546}$ & $\mathbf{0.222386\pm0.005230}$ & $\mathbf{0.839596\pm0.002879}$ & $\mathbf{0.906995\pm0.003416}$ & 241,675 \\
    \path{mcldnn}\cite{xu2020mcldnn} & I/Q & $0.250083\pm0.275698$ & $0.199852\pm0.319911$ & $0.250083\pm0.275698$ & $0.131307\pm0.069971$ & $0.318232\pm0.393735$ & $0.340303\pm0.431963$ & 406,199 \\
    \path{lwamcnet}\cite{wang2021lwamcnet} & I/Q & $0.551091\pm0.001838$ & $0.557940\pm0.008022$ & $0.551091\pm0.001838$ & $0.202538\pm0.008513$ & $0.750960\pm0.006679$ & $0.815960\pm0.003467$ & 20,395 \\
    \path{iq_param_matched} & I/Q & $0.594462\pm0.000468$ & $0.612415\pm0.004565$ & $0.594462\pm0.000468$ & $0.216345\pm0.007061$ & $0.812146\pm0.007270$ & $0.880934\pm0.003472$ & 136,395 \\
    \path{gated_fusion_iq_stft} & I/Q+STFT & $0.571947\pm0.002025$ & $0.585340\pm0.004474$ & $0.571947\pm0.002025$ & $0.211307\pm0.006707$ & $0.784015\pm0.004843$ & $0.840732\pm0.004582$ & 134,780 \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{fig04_accuracy_by_snr_group.pdf}
  \caption{9 个模型在 4 个 SNR 分组下的准确率对比（三种子均值±std，main\_table\_metrics.csv）。}
  \label{fig:accuracy-by-snr-group}
\end{figure}

在当前固定协议内，\path{cldnn} 的整体平均准确率为 0.612932，宏平均 F1 为 0.633238，是 9 个模型行中观察到的最高结果。主要 I/Q 基线中，\path{resnet1d} 的整体准确率为 0.595917，参数匹配控制模型 \path{iq_param_matched} 为 0.594462，二者与 CLDNN 的差距分别约为 1.70 和 1.85 个百分点。该结果说明本实验矩阵中的固定划分表现。

\path{iq_param_matched} 与 \path{resnet1d} 的整体准确率几乎相等，分别为 0.594462 和 0.595917，因此它的价值并不在于形成新的强基线排序，而在于作为融合解释的边际控制。它只使用 I/Q 输入，同时参数量接近双视图融合模型；当 \path{fusion_iq_stft} 相对它在低信噪比范围有小幅正差值、整体范围又为负差值时，这个对照能帮助区分“额外参数规模”与“新增 STFT 视图”带来的影响。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{fig11_full_snr_accuracy_macro_f1.pdf}
  \caption{代表模型在 $-20$ 至 $+18$ dB 的逐 SNR 准确率与宏平均 F1（三种子聚合，metrics\_per\_snr.csv）。}
  \label{fig:full-snr-accuracy-macro-f1}
\end{figure}

图~\ref{fig:full-snr-accuracy-macro-f1} 显示，静态 I/Q--STFT 融合在低信噪比区间接近 CLDNN，但在中高信噪比区间没有保持同等优势。门控融合曲线也未相对静态融合形成稳定改善。因此，主结果首先支持的是“不同信噪比区间存在性能权衡”，而不是“融合结构整体优于 I/Q 模型”。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.96\textwidth]{fig12_model_class_accuracy_heatmap.pdf}
  \caption{9 个模型 $\times$ 11 类调制的整体类别准确率热力图（metrics\_per\_class.csv）。}
  \label{fig:model-class-accuracy-heatmap}
\end{figure}

图~\ref{fig:model-class-accuracy-heatmap} 从类别维度补充表~\ref{tab:course-main-results-excerpt}。不同模型在 AM、PSK、QAM 和 WBFM 等类别上的表现并不均匀，说明整体准确率不能完全代表每一类调制方式的识别难度。低信噪比分析也应结合类别混淆结构解释。

\section{整体性能的配对检验}

表~\ref{tab:course-cldnn-overall-tests} 给出 CLDNN 与两个主要 I/Q 参照模型的整体准确率配对检验。差值定义为前一个模型准确率减去后一个模型准确率。

\begin{table}[htbp]
  \centering
  \caption{CLDNN 与主要 I/Q 参照模型的整体准确率配对检验}
  \label{tab:course-cldnn-overall-tests}
  \small
  \resizebox{\textwidth}{!}{%
  \begin{tabular}{lcccc}
    \toprule
    比较 & 范围 & 准确率差值 & 95\% 置信区间 & McNemar 检验 \\
    \midrule
    \path{cldnn} vs. \path{resnet1d} & 整体测试集 & 0.017015 & [0.015318, 0.018697] & \(p=1.52\times10^{-87}\) \\
    \path{cldnn} vs. \path{iq_param_matched} & 整体测试集 & 0.018470 & [0.016788, 0.020159] & \(p=2.86\times10^{-104}\) \\
    \bottomrule
  \end{tabular}%
  }
\end{table}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{fig06_bootstrap_delta_forest.pdf}
  \caption{已登记模型对配对自助法的准确率差值与 95\% CI（10000 重采样，paired\_bootstrap\_accuracy\_deltas.csv）。差值方向为 model\_a $-$ model\_b。}
  \label{fig:bootstrap-delta-forest}
\end{figure}

两项比较的置信区间均未跨越 0，说明 CLDNN 相对 \path{resnet1d} 和 \path{iq_param_matched} 的整体优势在当前配对样本上得到统计支持。McNemar 检验提供同一样本正确性方向差异的补充证据。统计表覆盖已登记模型对；表外模型对留给后续完整检验。

多重比较方面，本文将统计包中的 6 对已登记模型比较作为比较族，并分别考察 overall 与低信噪比两个范围。Holm--Bonferroni 修正后\cite{holm1979sequential}，overall 范围内 6 对比较均仍达到显著；低信噪比范围内，\path{fusion_iq_stft} vs. \path{cldnn} 的差值置信区间跨 0 且 McNemar \(p=0.476423\)，保持未显著，其余 5 对仍达到显著。该修正不改变主要解释：CLDNN 的整体优势稳定，静态融合相对 CLDNN 的低信噪比优势没有统计支持，门控融合相对静态融合为负结果。

\section{低信噪比下的融合权衡}

低信噪比区域定义为 \lowSNR{}。在这一范围内，\path{cldnn} 的平均准确率为 0.222386，\path{fusion_iq_stft} 为 0.221326，\path{iq_param_matched} 为 0.216345，\path{gated_fusion_iq_stft} 为 0.211307。静态融合接近 CLDNN，并高于参数匹配 I/Q 控制模型，但差异幅度较小。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.92\textwidth]{fig05_low_snr_per_snr_curve.pdf}
  \caption{代表模型在低信噪比区间（\lowSNR{}）的逐 SNR 准确率曲线（low\_snr\_table.csv）。}
  \label{fig:low-snr-per-snr}
\end{figure}

\subsection{低信噪比互补性图谱分析}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\textwidth]{fig14_paired_disagreement_by_snr.pdf}
  \caption{关键模型对在各 SNR 点的配对正确性净分歧（``仅 A 正确'' 占比 $-$ ``仅 B 正确'' 占比，predictions\_test.csv 三种子聚合）。}
  \label{fig:paired-disagreement-by-snr}
\end{figure}

图~\ref{fig:paired-disagreement-by-snr} 是解释低信噪比融合权衡的核心证据。它不是把两个模型的平均准确率相减，而是统计同一测试样本上“模型 A 正确、模型 B 错误”和相反方向的分歧比例，因此可以观察互补性出现在哪些 SNR 点。若 STFT 视图提供稳定互补，静态融合相对 I/Q 参照模型应在低信噪比区间持续出现正向净分歧；实际结果更像局部互补与局部损失的混合图谱。

相对 \path{iq_param_matched}，静态融合在 \(-14\)、\(-12\)、\(-10\) dB 的净分歧分别约为 \(+1.08\)、\(+1.74\)、\(+1.94\) 个百分点，说明 STFT 视图确实在部分低信噪比点补充了 I/Q 控制模型漏判的样本。但在 \(-8\) 和 \(-6\) dB，净分歧转为 \(-0.80\) 和 \(-0.36\) 个百分点，表明这种补充没有覆盖整个低信噪比区间。相对 \path{cldnn} 时，静态融合在 \(-14\) 和 \(-10\) dB 有 \(+1.35\) 和 \(+1.24\) 个百分点的局部正向净分歧，却在 \(-8\) 和 \(-6\) dB 出现 \(-1.77\) 和 \(-1.76\) 个百分点的反向分歧。这解释了为什么低信噪比总差值为 \(-0.001061\) 且置信区间跨 0：融合不是全局胜出，而是在不同 SNR 点上互相抵消。

门控融合的分歧图进一步强化负结果判断。相对静态融合，门控融合在 \(-16\) 到 \(-6\) dB 多数点为负向净分歧，其中 \(-10\) dB 约为 \(-2.50\) 个百分点，\(-12\) dB 约为 \(-1.65\) 个百分点。这说明当前标量门控没有学到“低信噪比时增加 STFT、较高信噪比时回退 I/Q”的有效策略，反而在静态融合已有局部收益的位置损失了样本。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{fig13_low_snr_class_recall_delta.pdf}
  \caption{静态 fusion\_iq\_stft 相对 iq\_param\_matched 与 CLDNN 在低 SNR 各调制类别的召回率差值 $\Delta$recall（confusion\_low\_snr.csv 三种子聚合）。}
  \label{fig:low-snr-class-recall-delta}
\end{figure}

\input{tables/low_snr_key_comparisons}

相对于 CLDNN，静态 I/Q--STFT 融合的低信噪比准确率差值为 \(-0.001061\)，95\% 置信区间为 \([-0.003939,0.001838]\)，区间跨越 0；McNemar 检验 \(p=0.476423\)。这组证据支持“接近但未优于 CLDNN”的解释。

相对于参数匹配 I/Q 控制模型，静态融合的低信噪比准确率差值为 0.004981，95\% 置信区间为 \([0.001970,0.007973]\)，McNemar 检验 \(p=0.001063\)。但同一比较在整体测试集上的准确率差值为 \(-0.017364\)，95\% 置信区间为 \([-0.019288,-0.015470]\)。因此，合理结论是：STFT 视图在本实现中提供了低信噪比局部小幅补充，但这一补充伴随整体准确率损失。

图~\ref{fig:low-snr-class-recall-delta} 进一步说明，静态融合的低信噪比差异并非对所有调制类别同向。相对 \path{iq_param_matched}，静态融合在 QAM16 上减少 \(7.4\) 个百分点召回率，却在 QAM64 上增加 \(8.9\) 个百分点召回率，接近等幅、方向相反。一个合理解释是，QAM16 与 QAM64 在低信噪比下星座点云重叠较强，STFT 幅度视图强调局部频谱能量后，分类器的 QAM 判别边界整体向 QAM64 一侧偏移：更多 QAM64 被保留下来，但部分 QAM16 被吸收到相邻区域。因此，该图更支持“类别间召回率交换”而非“所有类别共同受益”。附录图~\ref{fig:low-snr-confusion-panels} 给出代表模型在低信噪比范围内的行归一化混淆矩阵，作为类别混淆解释的补充。

\section{门控融合负结果}

\path{gated_fusion_iq_stft} 的整体准确率为 0.571947，低信噪比准确率为 0.211307，均低于静态融合 \path{fusion_iq_stft}。配对检验也支持这一判断：门控融合相对静态融合的整体准确率差值为 \(-0.005152\)，95\% 置信区间为 \([-0.006841,-0.003462]\)，McNemar 检验 \(p=2.43\times10^{-9}\)；低信噪比范围内的差值为 \(-0.010019\)，95\% 置信区间为 \([-0.012746,-0.007292]\)，McNemar 检验 \(p=1.23\times10^{-12}\)。

由于差值定义为“门控融合减静态融合”，上述负值说明当前标量门控机制没有改善静态融合。本文因此将门控融合作为负结果保留，不写作“门控提升鲁棒性”或“门控融合优于静态融合”。

\section{复杂度与受控 CUDA 前向延迟}

复杂度与延迟结果来自 CUDA 设备上的受控前向传播测量。测量使用 50 次预热和 200 次计时迭代，批量大小为 1 和 256；表~\ref{tab:course-cuda-latency} 摘录与结果解释最相关的模型行。

\begin{table}[htbp]
  \centering
  \caption{模型参数量与受控 CUDA 前向延迟摘录}
  \label{tab:course-cuda-latency}
  \begin{tabularx}{\textwidth}{lrrrX}
    \toprule
    模型行 & 参数量 & 批量 1 延迟/ms & 批量 256 延迟/ms & 测量边界 \\
    \midrule
    \path{resnet1d} & 111,755 & 2.081249 & 2.098535 & 仅 CUDA 前向传播。 \\
    \path{iq_param_matched} & 136,395 & 1.250068 & 1.273155 & 仅 CUDA 前向传播。 \\
    \path{cldnn} & 241,675 & 1.306352 & 1.543833 & 仅 CUDA 前向传播。 \\
    \path{fusion_iq_stft} & 98,299 & 1.844381 & 1.857622 & STFT 预处理未计入。 \\
    \path{gated_fusion_iq_stft} & 134,780 & 2.535083 & 2.545334 & STFT 预处理未计入。 \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.86\textwidth]{fig07_accuracy_latency_params_bubble.pdf}
  \caption{9 个模型的整体准确率 vs.\ CUDA 批量 1 前向延迟，气泡大小 \(\propto\) 参数量（warmup 50 / measured 200，complexity\_latency\_table.csv）。STFT 预处理耗时未计入。}
  \label{fig:accuracy-latency-params}
\end{figure}

这些数值说明同一受控测量条件下不同模型的参数量和 CUDA 前向耗时背景。例如，CLDNN 参数量较高但前向延迟并非最高；门控融合参数量低于 CLDNN，却具有更高的受控前向延迟。CPU 延迟、FLOPs/MACs 和 STFT 预处理完整耗时需要独立测量协议支撑。

\section{MCLDNN 异常与负结果保留}

\path{mcldnn} 的整体准确率为 \(0.250083\pm0.275698\)，宏平均 F1 为 \(0.199852\pm0.319911\)，标准差显著高于其他模型行。三个训练种子中，seed 42 的测试准确率为 0.568432，而 seed 2025 和 seed 3407 的测试准确率均为 0.090909，宏平均 F1 均为 0.015152，表现为近似随机水平的单类预测。

\begin{table}[htbp]
  \centering
  \caption{MCLDNN 异常训练种子的测试集预测分布}
  \label{tab:mcldnn-prediction-collapse}
  \begin{tabular}{lccc}
    \toprule
    训练种子 & 主要预测类别 & 预测样本数 & 现象判断 \\
    \midrule
    2025 & 8PSK（类别 0） & 44,000 / 44,000 & 单类塌陷 \\
    3407 & AM-DSB（类别 1） & 44,000 / 44,000 & 单类塌陷 \\
    \bottomrule
  \end{tabular}
\end{table}

表~\ref{tab:mcldnn-prediction-collapse} 进一步确认，seed 2025 并不是“多数类别偏置”，而是全部测试样本均预测为 8PSK；seed 3407 则全部预测为 AM-DSB。该现象与一个 11 类均衡测试集上的 0.090909 准确率一致，属于单类塌陷而非普通方差波动。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{fig15_seed_stability_all_models.pdf}
  \caption{9 个模型在 3 个训练种子上的整体与低信噪比准确率稳定性散点（metrics\_test.json）。}
  \label{fig:seed-stability-all-models}
\end{figure}

本文保留这两个异常种子，因为它们属于已登记的固定协议运行。删除异常种子并重算主表会改变实验协议，也会掩盖训练稳定性问题。协议纪律要求主表保留 3 个训练种子的完整结果；研究责任则要求对失败形态给出可检验解释，避免把异常简单归为“随机坏运气”。

当前可提出三类诊断假设。第一，学习率与权重衰减组合可能不适合 MCLDNN：全模型统一使用学习率 \(10^{-3}\)、权重衰减 \(10^{-4}\)、batch size 256，有利于协议对齐，但对多分支卷积、融合卷积和 LSTM 串联的结构未必公平。第二，初始化敏感性可能更强：seed 42 正常收敛而 seed 2025/3407 塌陷，提示某些初始门控、卷积核或循环层状态可能把梯度引向退化分类头。第三，输入格式或维度排列可能与该实现的结构假设不完全匹配：MCLDNN 常依赖对 I/Q 分支、时间维和通道维的特定组织方式，若实现中的 reshape、permute 或分支拼接与预期不一致，模型可能在部分初始化下无法形成有效类别边界。

因此，沿用统一超参对 MCLDNN 公平性不足。统一学习率、权重衰减和 batch size 的优点是避免为单个模型做事后调参，保证主表比较纪律；缺点是把结构稳定性、优化敏感性和超参适配能力混在一起。更负责的后续诊断应预先登记 MCLDNN 专用学习率网格、初始化重复、梯度范数监控、输入张量形状单元测试和早期 logits 分布检查，并在新的输出目录中形成独立证据。附录图~\ref{fig:mcldnn-seed-anomaly} 单独展示 MCLDNN 三个训练种子的差异。

\section{扩展实验与提案模型}\label{sec:results-extended}

本节扩展实验在 RTX 3090 上完成，与 Stage 5A 的 RTX 4070 硬件不同；跨硬件的样本级配对统计检验因此不可直接执行，三种子均值层面的解释仍受支持。所有扩展实验仅在本节用作协议化解释证据，不修改主表数值与统计检验。

\subsection{扩展实验摘要}\label{sec:results-extended-summary}

四类敏感性／诊断扩展实验（延长预算、低信噪比加权交叉熵、扩展复杂度、低信噪比混淆后处理）的发现汇总于表~\ref{tab:extended-summary}；详细每 cell 数据见同名独立证据标签的报告文档。

\begin{table}[htbp]
  \centering
  \caption{四类扩展实验摘要。Δ 列均为相对 Stage 5A 同模型的整体准确率均值变化。}
  \label{tab:extended-summary}
  \small
  \begin{tabularx}{\textwidth}{lXcX}
    \toprule
    证据标签 & 模型范围 / 干预 & Δ overall & 主要发现 \\
    \midrule
    \texttt{EXTENDED\_BUDGET\_3090} & 4 模型 \(\times\) 3 seed；epoch \(50\) + 5 轮 warmup + cosine annealing & \(\pm 1\) pp 内 & 排除欠训练假设；最佳轮 15--42，未触上限。 \\
    \texttt{LOW\_SNR\_WEIGHTED\_CE\_3090} & 2 模型 \(\times\) 3 seed；分组 SNR 加权 CE (low\(=2.0\), mid\(=1.0\), high\(=0.7\)) & cldnn \(-2.3\)，fusion \(-0.7\) & 低 SNR 微涨 \(+0.4\)--\(0.6\) pp，但中高 SNR 跌 \(2\)--\(4\) pp；不合算干预。 \\
    \texttt{CONTROLLED\_LATENCY\_EXTENDED} & 9 模型；CPU 单线程前向延迟 + thop MACs，warmup 50/measured 200 & — & 见表~\ref{tab:extended-complexity-cpu-latency}；fusion\_iq\_stft MACs (2.0 M) \textbf{少于} resnet1d (4.9 M)。 \\
    Stage 5A predictions 后处理 & 9 模型 \(\times\) 3 seed；行归一化低信噪比混淆矩阵 & — & 8 稳定模型在 \lowSNR{} 共同退化为偏置预测 AM-SSB（数字调制 \(\to\) AM-SSB 比例 0.63--0.81）；构成信息论级下限。 \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{table}[htbp]
  \centering
  \caption{扩展复杂度（\texttt{thop} MACs）与单线程 CPU 前向延迟}
  \label{tab:extended-complexity-cpu-latency}
  \scriptsize
  \setlength{\tabcolsep}{4pt}
  \begin{tabularx}{\textwidth}{lrrrr}
    \toprule
    模型行 & 参数量 & MACs（单样本） & CPU bs=1 / ms & CPU bs=256 / ms \\
    \midrule
    \path{tfcnn_stft} & 24,123 & 424,704 & 0.363 & 21.992 \\
    \path{cnn1d} & 37,131 & 1,553,920 & 0.411 & 61.285 \\
    \path{lwamcnet} & 20,395 & 1,504,096 & 2.327 & 52.871 \\
    \path{fusion_iq_stft} & 98,299 & 2,015,488 & 0.834 & 83.240 \\
    \path{gated_fusion_iq_stft} & 134,780 & 2,051,712 & 0.996 & 52.292 \\
    \path{iq_param_matched} & 136,395 & 4,945,408 & 0.688 & 74.088 \\
    \path{resnet1d} & 111,755 & 4,929,024 & 0.891 & 90.929 \\
    \path{cldnn} & 241,675 & 8,668,544 & 1.055 & 182.586 \\
    \path{mcldnn} & 406,199 & 49,097,472 & 2.994 & 572.462 \\
    \bottomrule
  \end{tabularx}
\end{table}

三条要点支撑后续提案模型解释。其一，延长预算不改写主表排序，也不缓解低信噪比退化，**Stage 5A 主表不是欠训练造成**。其二，分组 SNR 加权 CE 是不合算干预，单凭损失加权无法同时改善低信噪比与整体性能。其三，深低信噪比 (\(\le -16\) dB) 退化为偏置预测 AM-SSB 是信息论级失败下限，**没有信息量可供模型"更努力地学习"**；任何架构改动都不能改写这一下限。\path{fusion_iq_stft} MACs 仅为 \(2.0\) M（**少于** \path{resnet1d} 的 \(4.9\) M）这一事实，把 ``静态融合输给 CLDNN'' 的瓶颈定位到 I/Q 分支表征容量不足，为提案模型的架构升级提供动机。

\subsection{文献校准与可比 SOTA}\label{sec:results-literature}

表~\ref{tab:lit-sota-calibration} 列出可比文献的报告值，区分 avg-all-SNR（\(-20\) 至 \(+18\) dB 共 20 点平均，与本文 \texttt{overall\_accuracy} 同口径）、avg \(\mathrm{SNR}>0\)（\(+2\) 至 \(+18\) dB 共 9 点平均）与 peak（单 SNR 最佳点）三种口径，避免标题党的 ``\(95\%\)+'' 或 ``\(92\%\)'' 单点结果与本文 \texttt{overall\_accuracy} 错配。

\begin{table}[htbp]
  \centering
  \caption{近期文献在 \datasetname{} 上的可比性能校准。``avg-all-SNR'' 是 \(-20\) 至 \(+18\) dB 共 20 点平均，与本文 \texttt{overall\_accuracy} 同口径；``avg \(\mathrm{SNR}>0\)'' 是 9 点平均（\(+2\) 至 \(+18\) dB）；``peak'' 是单 SNR 最佳点。原论文未给出对应口径数字时记 ``--''。}
  \label{tab:lit-sota-calibration}
  \scriptsize
  \setlength{\tabcolsep}{3pt}
  \begin{tabularx}{\textwidth}{Xrrrl}
    \toprule
    方法（来源） & avg-all-SNR & avg \(\mathrm{SNR}>0\) & peak & 备注 \\
    \midrule
    LENet-M\cite{liu2024lenetm} & 0.6463 & -- & -- & 轻量 CNN+循环；2024 DSP \\
    SigFormer\cite{cao2024sigformer} & 0.6371 & -- & -- & 金字塔 Transformer；2023 IEEE \\
    ICRNNA\cite{li2024icrnna} & 0.6324 & -- & -- & RNN+attention；2024 \\
    CC-MSNet\cite{wang2024ccmsnet} & 0.6286 & -- & -- & 复值多流；2024 Sci Rep \\
    MCLDNN\cite{xu2020mcldnn} & -- & 0.9112 & 0.9227 & 原论文与 AMR-Benchmark\cite{zhangxx2022amrbenchmark} \\
    LSTM2\cite{zhangxx2022amrbenchmark} & -- & -- & 0.9205 & AMR-Benchmark \\
    PET-CGDNN\cite{zhang2021petcgdnn,zhangxx2022amrbenchmark} & -- & -- & 0.916 & AMR-Benchmark \\
    CLDNN2\cite{zhangxx2022amrbenchmark} & -- & -- & 0.908 & AMR-Benchmark \\
    \midrule
    本文 \path{cldnn}（Stage 5A） & 0.6129 & 0.9044 & 0.9138 & 三种子均值，本文 \\
    本文 \path{fusion_iq_stft}（Stage 5A） & 0.5771 & 0.8402 & 0.8482 & 三种子均值，本文 \\
    本文 \path{fusion_cldnn_stft}+aug+LS（提案） & \textbf{0.6264} & \textbf{0.9237} & \textbf{0.9327} & 三种子均值，本文，第~\ref{sec:results-fusion-cldnn-stft}~节 \\
    \bottomrule
  \end{tabularx}
\end{table}

跨论文比较受不同划分、不同测试集大小、不同种子和不同聚合口径影响，不构成严格 head-to-head 比较。本文以表~\ref{tab:lit-sota-calibration}~作为同量级参照：本文 CLDNN 0.6129 在 avg-across-SNR 维度上与 LENet-M 0.6463 和 SigFormer 0.6371 仍有 \(2\)--\(3\) 个百分点差距，提示固定协议下尚有 SOTA 级别可改进空间。

\subsection{提案模型 \texttt{fusion\_cldnn\_stft}+增强+标签平滑}\label{sec:results-fusion-cldnn-stft}

提案模型 \path{fusion_cldnn_stft}（第~3~章）在 P1.1 训练计划上叠加相位旋转加循环时移信号域增强（仅训练）与 \(\epsilon=0.1\) 标签平滑，登记为证据标签 \texttt{FUSION\_CLDNN\_STFT\_AUG\_LS\_3090}。表~\ref{tab:proposed-fusion-cldnn-stft-results}~给出三种子均值与标准差，并与 Stage 5A 的 CLDNN、Stage 5A 的静态 \path{fusion_iq_stft}、以及同训练计划下的扩展预算 \path{fusion_iq_stft} 三类参照逐项对照。

\begin{table}[htbp]
  \centering
  \caption{提案模型与三类参照的同协议比较（三种子均值与标准差，单位：准确率）}
  \label{tab:proposed-fusion-cldnn-stft-results}
  \small
  \begin{tabularx}{\textwidth}{Xcccc}
    \toprule
    模型行（参照／提案） & 整体 & 低 SNR & 中 SNR & 高 SNR \\
    \midrule
    \path{fusion_iq_stft}（Stage 5A） & \(0.5771\) & \(0.2213\) & \(0.7858\) & \(0.8428\) \\
    \path{fusion_iq_stft}（\texttt{EXTENDED\_BUDGET\_3090}） & \(0.5851\) & \(0.2124\) & \(0.8025\) & \(0.8646\) \\
    \path{cldnn}（Stage 5A，最强稳定基线） & \(0.6129\) & \(0.2224\) & \(0.8396\) & \(0.9070\) \\
    \path{fusion\_cldnn\_stft}+aug+LS（提案） & \(\mathbf{0.6264\pm0.0005}\) & \(\mathbf{0.2258\pm0.0008}\) & \(\mathbf{0.8606\pm0.0009}\) & \(\mathbf{0.9264\pm0.0003}\) \\
    \midrule
    \(\Delta\) 提案 vs.\ Stage 5A CLDNN & \(\mathbf{+0.0135}\) & \(+0.0034\) & \(+0.0210\) & \(+0.0194\) \\
    \(\Delta\) 提案 vs.\ Stage 5A \path{fusion_iq_stft} & \(+0.0493\) & \(+0.0045\) & \(+0.0748\) & \(+0.0836\) \\
    \(\Delta\) 提案 vs.\ \texttt{EXTENDED\_BUDGET} \path{fusion_iq_stft} & \(+0.0413\) & \(+0.0134\) & \(+0.0581\) & \(+0.0618\) \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{fig22_proposed_vs_stage5a_delta_heatmap.pdf}
  \caption{提案模型相对 Stage 5A 九个基线在四个指标（Overall / Low / Mid / High SNR）上的准确率增量热力图（单位：百分点；绿色正增量，红色负增量；★ 行为 CLDNN 基线加粗边框；数据源：三种子均值 \texttt{metrics\_test.json} vs \texttt{main\_table\_metrics.csv}）。}
  \label{fig:proposed-vs-stage5a-delta}
\end{figure}

提案模型在四个指标上同时正向超过 Stage 5A CLDNN 基线：整体 0.6264 vs 0.6129（$\Delta=+1.35$ pp，$\approx 27\sigma$，不属于种子噪声），低 / 中 / 高 SNR 均正向（图~\ref{fig:proposed-vs-stage5a-delta}）；最佳轮次 31 / 42 / 31，均位于 50 轮上限之内，证明模型已收敛。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.92\textwidth]{fig17_proposed_per_snr_comparison.pdf}
  \caption{提案模型与三类对照的逐 SNR 准确率曲线（三训练种子均值）。灰/绿阴影区分别为低 SNR (\(\le -6\) dB) 与高 SNR (\(\ge 8\) dB) 范围。}
  \label{fig:proposed-per-snr}
\end{figure}

与文献参照的对比（表~\ref{tab:lit-sota-calibration}）说明，提案模型在 avg-all-SNR 维度的 \(0.6264\) 落入 2024 年代多流／复值文献基线的同一量级（CC-MSNet \(0.6286\)、CCTL-Net \(0.6297\)），与 \path{LENet-M}（\(0.6463\)）、SigFormer（\(0.6371\)）仍有约 \(1\)--\(2\) 个百分点差距，记入未来工作；avg(\(\mathrm{SNR}>0\)) 维度的 \(0.9237\) 超过 MCLDNN 原论文报告的 \(0.9112\)，三种子 peak 均值 \(0.9327\) 与 MCLDNN 0.9227、LSTM2 0.9205、PET-CGDNN 0.916 等 AMR-Benchmark 报告 peak 比较，本文提案模型同口径上在该比较族中处于较高位置。需要强调的是，跨论文 peak 比较受不同划分、不同测试集大小和不同种子聚合口径影响，不构成严格 head-to-head 比较。

但本结果同时受到几条解释边界约束：第一，提案模型由架构升级、信号域增强与标签平滑三项干预联合构成，需要对三项干预做单干预消融才能拆分各自贡献；本报告在第~\ref{sec:proposed-ablation}~节给出该消融。第二，与 Stage 5A 的硬件不同，样本级配对自助法和 McNemar 检验无法跨硬件直接做，本表仅在三种子均值层面解释。第三，深低信噪比（\(\le -16\) dB）准确率仍约为 \(1/11\) 的随机水平，与本节前面的“崩塌为 AM-SSB”分析一致——提案模型不解决信息论级失败下限。这些边界与未来工作方向在第~5~章末尾说明。

\subsection{提案模型单干预消融与配对统计检验}\label{sec:proposed-ablation}

为隔离 +1.35 pp 增量中各干预贡献，本文在证据标签 \texttt{FUSION\_CLDNN\_STFT\_ABLATION\_3090} 下登记三变体（\texttt{arch\_only} / \texttt{arch\_aug} / \texttt{arch\_ls}），共享相同硬件、固定划分与三训练种子，仅在增强与标签平滑开关上区分；配对自助法（10000 重采样）与 McNemar 检验在整体集与低 SNR 子集分别执行（数据源：三种子 \texttt{metrics\_test.json} 与 \texttt{paired\_bootstrap.csv}）。

\input{tables/ablation_results}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{fig19_ablation_grouped_bars.pdf}
  \caption{四个消融变体（\texttt{arch\_only} / \texttt{arch\_aug} / \texttt{arch\_ls} / \texttt{full}）在 Overall / Low / Mid / High SNR 分组上的准确率均值 ± 标准差（三种子，数据源：\texttt{metrics\_test.json}）。每柱顶标注该分组最优值（加粗），水平虚线=Stage 5A CLDNN 整体基线（0.6129）。}
  \label{fig:ablation-grouped-bars}
\end{figure}

图~\ref{fig:ablation-grouped-bars} 直观呈现四个变体在各 SNR 分组的差距；详细配对证据见表~\ref{tab:proposed-paired-tests} 及图~\ref{fig:proposed-paired-forest}。

\input{tables/paired_tests_summary}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.92\textwidth]{fig20_proposed_paired_forest.pdf}
  \caption{提案模型与各参照的配对自助法差值（Δ）及 95\% CI 森林图（$\bullet$ CI 不跨 0，$\circ$ CI 跨 0；数据源：\texttt{results/paper\_stage6/proposed\_paired\_tests/paired\_bootstrap.csv}）。}
  \label{fig:proposed-paired-forest}
\end{figure}

综合图~\ref{fig:ablation-grouped-bars}、图~\ref{fig:proposed-paired-forest}与两张消融表，可提炼三条结论：\textbf{架构升级（\texttt{arch\_only}，均值 0.6116）}仅把 fusion 拉到 CLDNN 量级（$\Delta=-0.13$ pp，种子噪声内），本身无显著正向贡献；\textbf{信号域增强是主要涨点来源}，\texttt{arch\_aug} 达到 0.6284，相对 \texttt{arch\_only} 提升 +1.68 pp，配对检验 $p\approx 6\times10^{-78}$，低、中、高 SNR 均一致正向；\textbf{标签平滑在已有增强时冗余且略有害}，\texttt{full} 相对 \texttt{arch\_aug} 整体 $\Delta=-0.19$ pp（95\% CI $[-0.0033,-0.0006]$，McNemar $p=0.0155$），提案 +1.35 pp 可分解为增强 $\approx+1.55$ pp、架构 $\approx 0$ pp、标签平滑 $\approx -0.19$ pp。\texttt{arch\_aug}（0.6284）作为更简单配置略优于登记的 ``full''（0.6264），未来工作优先考虑取消标签平滑并细化增强强度网格；本节仍以 ``full'' 作为协议登记的提案主结果。
